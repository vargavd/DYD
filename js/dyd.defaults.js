/**
 * Created by VvD on 2016. 05. 24..
 */

/* Default options for DYD
 * If you want to modify, pls do it in a custom code, not here! */
$.fn.dyd.defaultSettings = {
    wrapperId: "dyd-wrapper",
    type: "barchart",
    showValue: true,
    drawShadow: true
};